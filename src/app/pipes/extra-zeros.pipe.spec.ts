import { ExtraZerosPipe } from './extra-zeros.pipe';

describe('ExtraZerosPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtraZerosPipe();
    expect(pipe).toBeTruthy();
  });
});
